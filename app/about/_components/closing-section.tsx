import { PageClosing } from "@/components/common/page-closing"
import { aboutContent } from "../_data"

export function AboutClosingSection() {
  return <PageClosing closing={aboutContent.closing} />
}