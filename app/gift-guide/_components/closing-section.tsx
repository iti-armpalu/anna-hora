import { PageClosing } from "@/components/common/page-closing"
import { giftGuideContent } from "../_data"

export function GiftGuideClosingSection() {
    return <PageClosing closing={giftGuideContent.closing} />
}