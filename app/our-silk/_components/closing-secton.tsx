import { PageClosing } from "@/components/common/page-closing"
import { silkContent } from "../_data"

export function SilkClosingSection() {
    return <PageClosing closing={silkContent.closing} />
}