import { PageClosing } from "@/components/common/page-closing"
import { sizeGuideContent } from "./_data"

export function SizeGuideClosingSection() {
    return <PageClosing closing={sizeGuideContent.closing} />
}