
import { PageClosing } from "@/components/common/page-closing"
import { careGuideContent } from "../_data"

export function CareGuideClosingSection() {
  return <PageClosing closing={careGuideContent.closing} />
}